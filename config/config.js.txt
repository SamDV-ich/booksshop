process.env.DEV = true;

exports.config = {
    server : {
        port : 8080,
        host : '127.0.0.1',
        requestTimeout : 60000 //in milliseconds
    },
    client : {
        dir : 'client',
        devDir : 'dev',
        prodDir : 'build',
        assets : 'assets'
    },
    view : {
        rootDir : 'client'
    }
};
